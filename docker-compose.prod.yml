services:
  salon-app:
    image: ghcr.io/des-sist-corp-ufpb/projeto-eq03/salon-app:latest
    container_name: salon-app
    env_file:
      - .env
    ports:
      - "80:8080"
    restart: always
    networks:
      - salon-network

volumes: {}

networks:
  salon-network:
    external: true
