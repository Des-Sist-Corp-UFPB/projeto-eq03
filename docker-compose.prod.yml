services:
  salon-app:
    image: ghcr.io/${IMAGE_REPO}/salon-app:latest
    container_name: salon-app
    env_file:
      - .env
    ports:
      - "80:8080"
    restart: always
    networks:
      - salon-network

volumes: {}

networks:
  salon-network:
    external: true
