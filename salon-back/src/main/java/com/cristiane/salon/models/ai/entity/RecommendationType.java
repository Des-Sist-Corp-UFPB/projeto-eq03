package com.cristiane.salon.models.ai.entity;

/** Tipos de recomendação suportados pelo motor de IA (peça compartilhada pela UI e pelo servidor MCP). */
public enum RecommendationType {
    FINANCEIRO,
    RETENCAO
}
